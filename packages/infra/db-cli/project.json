{
  "sourceRoot": "packages/infra/db-cli/src",
  "targets": {
    "pre-build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/infra/db-cli",
        "command": "rm -rf dist"
      }
    },
    "build": {
      "dependsOn": ["pre-build"],
      "outputs": ["{workspaceRoot}/packages/infra/db-cli/dist"],
      "options": {
        "cwd": "packages/infra/db-cli",
        "command": "tsc -p tsconfig.lib.json && tsc-alias -p tsconfig.lib.json"
      }
    }
  }
}
